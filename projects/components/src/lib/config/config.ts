/* Color predefinidos dentro de Material */
export enum MaterialColor {
  Primary = 'primary',
  Accent = 'accent',
  Warn = 'warn',
  Normal = 'normal'
}

/* Posiciones disponibles para Label */
export enum LabelPosition {
  Before = 'before',
  After = 'after'
}
